platform :ios, '13.0'

use_frameworks!

target 'RenalCalculi' do
  # Use prebuilt TensorFlowLite binary to avoid full Git clone
  pod 'TensorFlowLiteC', '~> 2.17.0'
  pod 'TensorFlowLiteSwift', '~> 2.17.0', :modular_headers => true
end
